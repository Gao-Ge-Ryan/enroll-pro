<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.gaogle.dao.master.EnterpriseSlideshowMapper">
    <resultMap type="top.gaogle.pojo.model.EnterpriseSlideshowModel" id="EnterpriseSlideshowMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="picture" column="picture" jdbcType="VARCHAR"/>
        <result property="redirectUrl" column="redirect_url" jdbcType="VARCHAR"/>
        <result property="enterpriseId" column="enterprise_id" jdbcType="VARCHAR"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createAt" column="create_at" jdbcType="INTEGER"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateAt" column="update_at" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="sqlAllColumns">
        id, picture, redirect_url, enterprise_id, create_by, create_at, update_by, update_at
    </sql>


    <insert id="insert">
        insert into enterprise_slideshow(<include refid="sqlAllColumns"/>)
        values
        (#{id},#{picture}, #{redirectUrl}, #{enterpriseId}, #{createBy}, #{createAt}, #{updateBy}, #{updateAt})
    </insert>
    <update id="put">
        update enterprise_slideshow
        <set>
            <if test="picture != null and picture != ''">
                picture = #{picture},
            </if>
            <if test="redirectUrl != null and redirectUrl != ''">
                redirect_url = #{redirectUrl},
            </if>
            <if test="enterpriseId != null and enterpriseId != ''">
                enterprise_id = #{enterpriseId},
            </if>
            <if test="createBy != null and createBy != ''">
                create_by = #{createBy},
            </if>
            <if test="createAt != null">
                create_at = #{createAt},
            </if>
            <if test="updateBy != null and updateBy != ''">
                update_by = #{updateBy},
            </if>
            <if test="updateAt != null">
                update_at = #{updateAt},
            </if>
        </set>
        where id = #{id}
    </update>
    <select id="queryByPageAndCondition" resultMap="EnterpriseSlideshowMap">
        select
        <include refid="sqlAllColumns"/>
        from enterprise_slideshow
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="picture != null and picture != ''">
                and picture = #{picture}
            </if>
            <if test="redirectUrl != null and redirectUrl != ''">
                and redirect_url = #{redirectUrl}
            </if>
            <if test="enterpriseId != null and enterpriseId != ''">
                and enterprise_id = #{enterpriseId}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createAt != null">
                and create_at = #{createAt}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
            <if test="updateAt != null">
                and update_at = #{updateAt}
            </if>
        </where>
    </select>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from enterprise_slideshow
        where id = #{id}
    </delete>
    <delete id="deleteByIdAndEnterpriseId">
        delete
        from enterprise_slideshow
        where id = #{id}
          and enterprise_id = #{enterpriseId}
    </delete>
    <delete id="deleteByEnterpriseId">
        delete
        from enterprise_slideshow
        where enterprise_id = #{enterpriseId}
    </delete>

    <!--查询单个-->
    <select id="queryById" resultMap="EnterpriseSlideshowMap">
        select
        <include refid="sqlAllColumns"/>
        from enterprise_slideshow
        where id = #{id}
    </select>
    <select id="queryByEnterpriseId" resultMap="EnterpriseSlideshowMap">
        select
        <include refid="sqlAllColumns"/>
        from enterprise_slideshow where enterprise_id = #{enterpriseId}
    </select>

</mapper>